# State

**Where we are (2026-08-13):** Spec written and committed —
`docs/superpowers/specs/2026-08-13-dgx-spark-experiments-design.md`.
Research done: playbooks repo cloned and summarized (46 Spark playbooks,
snapshot 2026-08-13), CES-2026 numbers scoped, vlm-finetuning withdrawal
verified with receipts. Nothing built, nothing deployed.

**PAUSED by request.** Next action: Ryan reviews the spec → on approval,
write implementation plan, then build data.js from the repo snapshot,
build the site, verify per spec §10, create GitHub repo + Vercel project
`dgxsparkexperiments`, deploy to dgxsparkexperiments.vercel.app.

Raw playbook summaries from the clone live in the session scratchpad
(regenerate anytime: shallow-clone NVIDIA/dgx-spark-playbooks, run a
summarizer over nvidia/*/README.md).
